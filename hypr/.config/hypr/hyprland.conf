# # Hyprland configuration generated from existing i3 configuration
# # Inspired by JaKooLit/Fedora-Hyprland
#
# # Mod key
# $mod = SUPER
#
# ###################################################################
# # Monitors
# ###################################################################
# # Primary laptop display
# monitor=eDP-1,preferred,auto,1
# # External HDMI monitor to the right of the laptop screen
# monitor=HDMI-1,1920x1080@60,auto,1
#
# ###################################################################
# # Autostart
# ###################################################################
# exec-once = nm-applet
# exec-once = ~/.machine/utils/ext_mon_toggle
# exec-once = xset -dpms
# exec-once = xset s off
# exec-once = xset s noblank
# exec-once = waybar
#
# ###################################################################
# # Keybindings
# ###################################################################
# # Applications
# bind = $mod, RETURN, exec, alacritty
# bind = $mod SHIFT, Q, killactive,
# bind = $mod, D, exec, rofi -modi drun,run -show drun
#
# # Audio controls
# bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +10%
# bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -10%
# bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
# bind = ,XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
#
# # Brightness controls
# bind = ,XF86MonBrightnessUp, exec, brightnessctl set +5%
# bind = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-
#
# # Window focus
# bind = $mod, H, movefocus, l
# bind = $mod, J, movefocus, d
# bind = $mod, K, movefocus, u
# bind = $mod, L, movefocus, r
#
# bind = $mod, SEMICOLON, movefocus, r
# bind = $mod, LEFT, movefocus, l
# bind = $mod, DOWN, movefocus, d
# bind = $mod, UP, movefocus, u
# bind = $mod, RIGHT, movefocus, r
#
# # Move windows
# bind = $mod SHIFT, H, movewindow, l
# bind = $mod SHIFT, J, movewindow, d
# bind = $mod SHIFT, K, movewindow, u
# bind = $mod SHIFT, L, movewindow, r
# bind = $mod SHIFT, SEMICOLON, movewindow, r
# bind = $mod SHIFT, LEFT, movewindow, l
# bind = $mod SHIFT, DOWN, movewindow, d
# bind = $mod SHIFT, UP, movewindow, u
# bind = $mod SHIFT, RIGHT, movewindow, r
#
# # Layout management
# bind = $mod, F, fullscreen, 1
# bind = $mod SHIFT, SPACE, togglefloating,
# bind = $mod, SPACE, togglefloating,
#
# # Workspaces
# bind = $mod, 1, workspace, 1
# bind = $mod, 2, workspace, 2
# bind = $mod, 3, workspace, 3
# bind = $mod, 4, workspace, 4
# bind = $mod, 5, workspace, 5
# bind = $mod, 6, workspace, 6
# bind = $mod, 7, workspace, 7
# bind = $mod, 8, workspace, 8
# bind = $mod, 9, workspace, 9
# bind = $mod, 0, workspace, 10
#
# bind = $mod SHIFT, 1, movetoworkspace, 1
# bind = $mod SHIFT, 2, movetoworkspace, 2
# bind = $mod SHIFT, 3, movetoworkspace, 3
# bind = $mod SHIFT, 4, movetoworkspace, 4
# bind = $mod SHIFT, 5, movetoworkspace, 5
# bind = $mod SHIFT, 6, movetoworkspace, 6
# bind = $mod SHIFT, 7, movetoworkspace, 7
# bind = $mod SHIFT, 8, movetoworkspace, 8
# bind = $mod SHIFT, 9, movetoworkspace, 9
# bind = $mod SHIFT, 0, movetoworkspace, 10
#
# bind = $mod SHIFT, C, exec, hyprctl reload
# bind = $mod SHIFT, R, exec, hyprctl reload
# bind = $mod SHIFT, E, exit,
#
# # Move current workspace to the monitor on the right
# bind = $mod, P, moveworkspacetomonitor, r
#
# ###################################################################
# # Resize mode
# ###################################################################
# bind = $mod, R, submap, resize
# submap = resize
# binde = ,J, resizeactive, -10 0
# binde = ,K, resizeactive, 0 10
# binde = ,L, resizeactive, 0 -10
# binde = ,SEMICOLON, resizeactive, 10 0
# binde = ,LEFT, resizeactive, -10 0
# binde = ,DOWN, resizeactive, 0 10
# binde = ,UP, resizeactive, 0 -10
# binde = ,RIGHT, resizeactive, 10 0
# bind = ,RETURN, submap, reset
# bind = ,ESCAPE, submap, reset
# submap = reset
#
# ###################################################################
# # Appearance settings from Fedora-Hyprland reference
# ###################################################################
# general {
#     gaps_in = 5
#     gaps_out = 10
#     border_size = 2
#     layout = dwindle
#     allow_tearing = false
# }
#
# decoration {
#     rounding = 5
#     drop_shadow = yes
#     shadow_range = 4
#
#     blur {
#         enabled = yes
#         size = 3
#     }
# }
#
# animations {
#     enabled = yes
# }



# Hyprland configuration - i3-like setup
$mod = SUPER

# Monitor setup
monitor=eDP-1,preferred,auto,1
monitor=HDMI-A-1,1920x1080@60,1920x0,1

# Autostart
exec-once = waybar
exec-once = mako
exec-once = hyprpaper
exec-once = hypridle

# i3-like keybindings
bind = $mod, Return, exec, alacritty
bind = $mod, d, exec, wofi --show drun
bind = $mod, q, killactive
bind = $mod SHIFT, e, exit
bind = $mod SHIFT, r, exec, hyprctl reload

# Window management (i3-like)
bind = $mod, h, movefocus, l
bind = $mod, j, movefocus, d
bind = $mod, k, movefocus, u
bind = $mod, l, movefocus, r

bind = $mod SHIFT, h, movewindow, l
bind = $mod SHIFT, j, movewindow, d
bind = $mod SHIFT, k, movewindow, u
bind = $mod SHIFT, l, movewindow, r

# Workspaces (i3-like)
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5

# Screenshots (better than i3)
bind = , Print, exec, grim -g "$(slurp)" - | wl-copy
bind = SHIFT, Print, exec, grim - | wl-copy

# Audio/brightness (same as i3)
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +10%
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -10%
bind = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = ,XF86MonBrightnessUp, exec, brightnessctl set +5%
bind = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Lock screen
bind = $mod, x, exec, hyprlock

# Layout toggles
bind = $mod, f, fullscreen
bind = $mod SHIFT, space, togglefloating

# env = XKB_CONFIG_ROOT,~/.config/xkb


input {
  kb_layout = real-prog-dvorak,us
  kb_options = ctrl:nocaps
    # kb_layout = real-prog-dvo-k,real-prog-dvorak,us,dvorak
    # kb_variant =
    # kb_model = pc105
    # kb_options = caps:ctrl_modifier,grp:alt_shift_toggle
    # kb_rules = evdev
    #
    # follow_mouse = 1
    # touchpad {
    #     natural_scroll = no
    # }
    # sensitivity = 0
}

# Keybinds for layout switching
bind = $mod, F1, exec, hyprctl switchxkblayout at-translated-set-2-keyboard 0  # real-prog-dvo-k
bind = $mod, F2, exec, hyprctl switchxkblayout at-translated-set-2-keyboard 1  # real-prog-dvorak
bind = $mod, F3, exec, hyprctl switchxkblayout at-translated-set-2-keyboard 2  # us
